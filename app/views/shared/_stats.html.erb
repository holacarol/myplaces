<% @user ||= current_user %>
<div class="stats">
  <table summary="User stats">
    <tr>
      <td>
        <a href="<%= friends_user_path(@user) %>">
          <span id="friends" class="stat">
            <%=pluralize(@user.friends.count, "friend") %>
          </span>
        </a>
      </td>
      <td>
        <a href="<%= pending_friends_user_path(@user) %>">
          <span id="pending_friends" class="stat">
            <%= pluralize(@user.pending_friends.count, "pending friend") %>
          </span>
        </a>
      </td>
      <td>
        <a href="<%= requested_friends_user_path(@user) %>">
          <span id="requested_friends" class="stat">
            <%= pluralize(@user.requested_friends.count, "requested friend") %>
          </span>
        </a>
      </td>

    </tr>
  </table>
</div>

